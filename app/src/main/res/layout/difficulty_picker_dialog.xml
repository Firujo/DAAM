<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="300dp"
    android:layout_height="fill_parent" >

  <NumberPicker
      android:id="@+id/numberPicker1"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_alignParentTop="true"
      android:layout_centerHorizontal="true"
      android:layout_marginTop="64dp" />

  <Button
      android:id="@+id/button_affirmative"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_below="@+id/numberPicker1"
      android:layout_marginStart="20dp"
      android:layout_marginTop="98dp"
      android:layout_toEndOf="@+id/numberPicker1"
      android:text="@string/ok" />

  <Button
      android:id="@+id/button_negative"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_alignBaseline="@+id/button_affirmative"
      android:layout_alignBottom="@+id/button_affirmative"
      android:layout_marginEnd="16dp"
      android:layout_toStartOf="@+id/numberPicker1"
      android:text="@string/cancel" />

</RelativeLayout>